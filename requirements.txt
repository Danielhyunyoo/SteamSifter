# Core dependencies for SteamSifter MVP
# Add more as we build (LLM SDK, etc.) in later steps.

requests==2.32.3       # HTTP calls to the Steam reviews API
python-dotenv==1.0.1   # Load the API key from a local .env file
google-genai==2.8.0    # Google Gemini SDK (AI classification)
pydantic==2.13.4       # Schemas for structured AI output
openai>=2.0.0           # OpenAI SDK (alternative AI provider)
flask>=3.0.0           # web app framework (V3)
gunicorn>=21.2.0       # production WSGI server (deployment)
flask-limiter>=3.5.0   # per-visitor rate limiting (protect the API key)
redis>=5.0.0           # persistent cache store (Upstash) on deploy
